<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        No special permissions are required for UPI payments.
        The app hands control to the user's UPI app (Google Pay, PhonePe,
        Paytm, etc.) through a standard ACTION_VIEW deep link — the UPI app
        performs the actual money transfer on behalf of the user.
        INTERNET is only needed if you later add a backend for transaction
        verification.
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UpiPayments">

        <!-- Single Activity hosting the Navigation graph. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
